
export interface UserProfile {
    id: string;
    fullName: string;
    email: string;
  }

  export interface TokenType {
    accessToken:string,
    refreshToken:string,
    tokenExpiresAt: any
  }
  

