export interface Order {
    _id: string
    projectId: string
    orderCode: string
    phoneNumber: string
    address: string
    carts: string
    createdAt: string
    totalBeforePromotion: string
    totalAftterPromotion: string
    isRead: boolean
  }
  